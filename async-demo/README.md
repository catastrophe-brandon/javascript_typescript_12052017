In-class exercise with NodeJS

# Most packages come with a command-line executables

Examples:

    ./node_modules/.bin/serve

# Conventions
package-lock.json - Locks down the dependencies.
node_modules
